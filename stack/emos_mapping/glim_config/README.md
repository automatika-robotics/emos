# GLIM configuration templates

Copies of `config/` from [GLIM](https://github.com/koide3/glim) v1.2.2 (MIT).
Installed to `share/emos_mapping/glim_config`. A mapping session copies them into the map directory and sets the topics, the CPU or CUDA variants, and the extension modules for that session.
