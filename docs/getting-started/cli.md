# EMOS CLI Reference

The `emos` CLI is the primary interface for installing, managing, and running recipes on the EMOS platform. It handles everything from initial environment setup to recipe discovery, download, and execution -- all from a single command-line tool.

For installation instructions, see the [Installation guide](installation.md). The complete CLI manual is included below.

```{include} ../../stack/emos-cli/MANUAL.md
```
